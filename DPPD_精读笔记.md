# DPPD: Learning Dynamic Prompts for All-in-One Image Restoration -- 精读笔记

> **论文信息**: IEEE Transactions on Image Processing, Vol. 34, 2025
> **作者**: Gang Wu, Junjun Jiang, Kui Jiang, Xianming Liu, Liqiang Nie (哈尔滨工业大学)
> **代码**: https://github.com/Aitical/DPPD

---

## 一、DPPD 方法论完整细节

### 1.1 问题动机

现有 all-in-one 图像复原方法主要依赖提取退化先验（degradation priors），通常使用学习和固定的任务提示（task prompts）来指导复原过程。但这些**静态提示**存在两个核心缺陷：

1. **判别性不足**: 以 PromptIR 为例，其 softmax 权重在多个退化任务上几乎均匀分布（Figure 1 展示的可视化），说明缺乏任务感知的判别性表示，不同退化类型间没有清晰边界。
2. **表达能力受限**: 推理时提示退化为固定参数，输入特征只能通过线性组合（softmax）映射到有限空间，严重限制了目标提示表示的范围（Figure 3(a) 的示意）。

### 1.2 整体框架：DPPD = DPA + PDL

DPPD（Degradation Prototype Assignment and Prompt Distribution Learning）将退化先验提取解耦为两个组件：

- **DPA（退化原型分配）**: 增强判别性
- **PDL（提示分布学习）**: 增强表达能力（动态性）

二者协同工作：DPA 提供判别性的退化锚定，PDL 在此基础上通过分布建模实现动态提示采样。

---

## 二、动态提示（Dynamic Prompts）与生成机制

### 2.1 什么是"动态提示"

与现有方法使用固定参数化的提示不同，DPPD 将提示建模为一个**条件高斯混合模型（Conditional Gaussian Mixture Model）**。每个提示不再是单一固定的向量，而是一个**分布**，可以从中采样出多样的提示表示。

**核心对比**（Figure 3）：
- **静态提示（Static Prompt）**: 自适应提示（彩色点）在推理时形成固定表示，输入查询（红点）仅通过线性组合投影到有限空间。
- **动态提示（Dynamic Prompts）**: 原型周围的重叠点表示从学习到的分布中采样的提示，实现了更丰富、更自适应的退化特定表示。

### 2.2 动态提示的生成过程

动态提示 P 的生成分为两步——**DPA 赋权重** + **PDL 采样**：

**第一步：退化原型分配（DPA）——计算混合权重**

1. 从编码器中间层提取退化图像的特征表示 $F \in \mathbb{R}^{H \times W \times C}$
2. 全局平均池化得到紧凑特征向量 $f \in \mathbb{R}^C$
3. 通过全连接层变换为退化嵌入 $\hat{z} \in \mathbb{R}^d$
4. 计算与每个原型 $p_k$ 的余弦相似度：
$$s_k = \cos(\hat{z}, p_k) = \frac{\hat{z} \cdot p_k}{|\hat{z}||p_k|}, \quad k = 1, 2, ..., K$$
5. 通过 softmax 计算分配权重（即混合模型的混合系数）：
$$\alpha_k = \frac{\exp(s_k)}{\sum_{j=1}^{K} \exp(s_j)}, \quad k = 1, 2, ..., K$$

**第二步：提示分布学习（PDL）——采样动态提示**

将提示建模为条件高斯混合分布：
$$p(P|\hat{z}) = \sum_{k=1}^{K} \alpha_k \cdot \mathcal{N}(P | \mu_k, \Sigma_k)$$

其中 $\mu_k$ 和 $\Sigma_k$ 是第 k 个高斯分量的均值和协方差，均为**可学习参数**，捕获每种退化类型内部的变异性。

使用**重参数化技巧（Reparameterization Trick）** 进行采样：
$$P = \sum_{k=1}^{K} \alpha_k (\mu_k + \Sigma_k^{1/2} \epsilon_k), \quad \epsilon_k \sim \mathcal{N}(0, I)$$

**关键设计思想**: 每个高斯分量 $\mathcal{N}(\mu_k, \Sigma_k)$ 与一个退化原型 $p_k$ 关联。分配权重 $\alpha_k$ 决定了每个退化分布对最终提示的贡献，实现输入自适应的动态生成。

---

## 三、先验知识蒸馏机制（Prompt-guided Distillation）

### 3.1 CrossFusion 模块——提示如何注入复原过程

CrossFusion 模块负责将 DPPD 生成的动态提示 $P \in \mathbb{R}^{N \times C}$（N 为提示向量数量，C 为特征维度）融入编码器特征图 $F \in \mathbb{R}^{H \times W \times C}$。

**具体流程**：

1. 将特征图 reshape 为特征向量序列 $F \in \mathbb{R}^{HW \times C}$
2. 构建交叉注意力（Cross-Attention）的三元组：
$$Q = W_Q F, \quad K = W_K P, \quad V = W_V P$$
   - **图像特征作为 Query**（主动查询需要什么信息）
   - **动态提示作为 Key 和 Value**（提供退化先验知识）
3. 计算交叉注意力：
$$F_{attn} = \text{softmax}\left(\frac{QK^T}{\sqrt{d_k}}\right) V$$
4. 输出投影：
$$F_{output} = W_O F_{attn}$$

5. 与 PromptIR 一致，随后接一个 Transformer 层进一步增强特征融合。

**本质**: 图像特征通过 Query 主动从动态提示中检索相关的退化先验信息，指导复原过程。这就是"先验知识蒸馏"——将退化的判别性先验知识通过交叉注意力机制注入到图像复原的特征空间中。

---

## 四、架构图描述（Figure 6）

### 4.1 Figure 6：DPPD 整体架构概览

**整体结构**: 基于 Transformer 的编码器-解码器骨干网络 + DPPD 模块 + CrossFusion 模块。

**数据流（从输入到输出）**：

```
退化图像 x
    │
    ▼
┌─────────────────────────────┐
│  Transformer Encoder         │
│  (多级特征提取)              │
│        │                     │
│        ├────── F (中间层特征) ────→ DPPD 模块
│        │                        │
│        │                    ┌───┴───┐
│        │                    │  DPA  │ ← 退化原型 {p_1,...,p_K}
│        │                    └───┬───┘
│        │                    ┌───┴───┐
│        │                    │  PDL  │ → 动态提示 P
│        │                    └───┬───┘
│        │                        │
│        │                  CrossFusion
│        │                  (交叉注意力)
│        │                        │
│        │            F' = F + CrossAttn(F, P)  ← 残差连接
│        │                        │
│        ├────────────────────────┘
│        │
│  Transformer Decoder
│        │
│        ▼
   复原图像 ŷ
```

**各模块角色**：

| 模块 | 功能 |
|------|------|
| **Encoder** | 多级特征提取，从退化图像中提取多尺度特征 |
| **DPA** | 将退化特征匹配到预定义原型，输出判别性分配权重 $\alpha_k$ |
| **PDL** | 基于分配权重从条件高斯混合模型采样，生成动态提示 P |
| **CrossFusion** | 通过交叉注意力 + 残差连接，将提示信息融入图像特征 |
| **Decoder** | 基于融合后的特征重建高质量图像 |

### 4.2 Figure 2：DPA 的判别效果可视化

- **(a) 自适应提示方法**: 退化特征在潜在空间中散乱、重叠分布，判别性差
- **(b) DPA 方法**: 引入锚定原型（蓝色、橙色、绿色星形）作为潜在空间的判别中心，使不同退化类型的特征分布更好地分离

### 4.3 Figure 3：静态 vs 动态提示对比

- **(a) 静态提示**: 推理时提示为固定点，查询点只能通过线性组合映射到有限空间
- **(b) 动态提示（DPPD）**: 原型周围有从分布中采样的重叠点，表示空间更丰富

### 4.4 Figure 5：图像复原方法演进

- (a) 单任务模型 → (b) 多分支架构 → (c) 两阶段模型（预训练提示） → (d) 自适应提示模型（PromptIR） → (e) **本文 DPPD**：引入判别性退化原型和提示分布学习

---

## 五、损失函数详情

### 5.1 重建损失

训练目标为最小化复原图像与真实图像之间的 L1 重建损失：

$$\mathcal{L}_{rec} = \frac{1}{N} \sum_{i=1}^{N} |\hat{y}_i - y_i|_1$$

其中 $y_i$ 为真实图像，$\hat{y}_i$ 为网络输出的复原图像，N 为训练样本数。

### 5.2 提示分布正则化（KL 散度损失）

从变分推断（Variational Inference）视角，PDL 可视为优化证据下界（ELBO）。引入标准高斯先验 $p(P) = \mathcal{N}(0, I)$，最小化学习分布与先验之间的 KL 散度：

$$\mathcal{L}_{KL} = KL(p(P|\hat{z}) \| p(P)) = \sum_{k=1}^{K} \alpha_k \cdot KL(\mathcal{N}(\mu_k, \Sigma_k) \| \mathcal{N}(0, I))$$

**作用**:
- 鼓励模型维持多样化的提示分布
- 防止分布偏离先验过远
- 起到正则化效果，避免过拟合

### 5.3 总损失

论文虽未明确写出总损失的加权公式，但训练过程同时优化 $\mathcal{L}_{rec}$ 和 $\mathcal{L}_{KL}$。

---

## 六、实验结果

### 6.1 Setting 1：三退化任务（去噪 + 去雨 + 去雾）

**数据集**:
- 去噪: 训练集 BSD400 + WED (5,144 张)，噪声级别 $\sigma \in \{15, 25, 50\}$；测试集 BSD68 + Urban100
- 去雨: Rain100L (200 训练/100 测试)
- 去雾: RESIDE (OTS 8,990 训练 / SOTS 500 测试)

**关键结果**（DPPD + PromptIR vs 原始 PromptIR）:
- 去噪（$\sigma=15, 25, 50$）: 各级别均显著提升，说明分布建模有效捕捉了不同噪声级别
- 去雨（Rain100L）: PSNR 超越原始 PromptIR
- 去雾（SOTS）: PSNR 超越原始 PromptIR

对比方法包括: MPRNet, Restormer, NAFNet, FSNet, DRSformer, MambaIR, DL, AirNet, IDR, Gridformer, NDR, InstructIR, Perceive-IR 等。

### 6.2 Setting 2：五退化任务（+ 去模糊 + 低光增强）

**新增数据集**:
- 去模糊: GoPro (2,103 训练 / 1,111 测试)，真实相机抖动和物体运动产生的运动模糊
- 低光增强: LOL (500 训练 / 15 测试)

**关键结果**:
- 去雨 Rain100L: PSNR = **37.32 dB**
- 去模糊 GoPro: PSNR = **28.74 dB**
- 低光增强 LOL: PSNR = **22.73 dB**, SSIM = **0.846**

对比方法涵盖任务特定方法（SwinIR, MIRNet-v2）、通用方法（Restormer, NAFNet）和 all-in-one 方法（DL, TAPE, AirNet, IDR, InstructIR, Perceive-IR）。

### 6.3 Setting 3：七退化任务（+ 超分辨率 + JPEG 去块）

**基础架构**: SRResNet（CNN）和 Uformer（Transformer）

**训练策略对比**:
- `-M`: 混合学习（所有退化混合训练）
- `-S`: 顺序学习
- `-EP`: 显式提示学习（预训练分类器特征）
- `-AP`: 自适应提示学习

**关键发现**:
- DPPD 在 SRResNet 和 Uformer 两种架构上均带来一致提升
- 覆盖 CNN 和 Transformer 两种范式，证明即插即用的通用性
- 显式提示（-EP）优于自适应提示（-AP），但 DPPD 进一步提升了两者

### 6.4 Setting 4：复合退化任务（CDD-11）

CDD-11 包含 11 种退化类别：
- 4 种单一退化: 低光(l)、雾(h)、雨(r)、雪(s)
- 7 种复合退化: l+h, h+r, l+h+r 等

**关键结果**:
- DPPD + PromptIR 平均 PSNR = **28.84 dB**，vs 原始 PromptIR **25.90 dB**
- **提升 +2.94 dB**，极为显著
- 复合退化表现尤为突出: 雾+雨 **+5.33 dB**，雾+雪 **+5.94 dB**
- 这验证了分布建模在处理复杂特征交互（混合退化场景）中的优势

---

## 七、消融实验关键发现

### 7.1 核心组件贡献（Table V, 基于 PromptIR, 3 任务）

| 配置 | DPA | PDL | 平均 PSNR (dB) | vs Baseline |
|------|-----|-----|-----------------|-------------|
| Baseline (PromptIR) | - | - | 32.06 | - |
| +DPA | ✓ | - | 32.35 | **+0.29** |
| +PDL | - | ✓ | 32.38 | **+0.32** |
| **DPPD (完整)** | ✓ | ✓ | **32.59** | **+0.53** |

**关键洞察**:
- DPA 和 PDL 各自独立均有显著提升
- **两者结合有协同效应**（0.29 + 0.32 < 0.53），说明判别性锚定和动态采样相互增强
- PDL 单独在去雾和去噪上优于 DPA 单独，但在去雨上略差（因为去雨和去噪的特征特性相似，纯 PDL 难以区分）
- 完整 DPPD 在所有退化类型上一致提升：去雾 +0.52 dB，去雨 +1.37 dB

### 7.2 原型初始化策略影响（Table VI, 基于 Uformer, 7 任务）

| 初始化方式 | 平均 PSNR (dB) |
|------------|-----------------|
| EP（预训练分类器特征） | 30.95 |
| EP + PDL | 31.40 |
| **DPPD（正交初始化 + PDL）** | **31.33** |

**关键洞察**:
- EP+PDL 略优于 DPPD（31.40 vs 31.33），因为监督预训练提供了丰富的退化表示
- 但 EP 依赖标注退化数据的预训练，无法泛化到未知退化类型
- DPPD 的正交初始化（max margin initialization）避免了预训练的计算开销，同时达到接近的性能，更加灵活通用
- PDL 加到 EP 上对比 EP 单独提升了 +0.45 dB，验证了分布学习的有效性

### 7.3 原型数量 K 的影响（Figure 10, 基于 PromptIR, 3 任务）

| K (原型数) | 平均 PSNR (dB) |
|------------|-----------------|
| K=3 | 较低 |
| K=5 | 较好 |
| **K=7** | **32.64（最佳）** |
| K=9 | 略有下降 |
| K=11 | 继续下降 |

**关键洞察**:
- 最优 K=7，略高于实验中的实际退化类型数（将不同噪声级别分开计为 5 种）
- 更丰富的表示空间有利于捕捉每种退化类型内的细微变化
- 但过多原型（K>7）会引入优化困难和边际递减
- 使用退化类型数作为参考点已经能在现有方法上取得显著提升
- DPPD 框架对 K 超参数表现出良好的鲁棒性

---

## 八、方法定位与局限性

### 8.1 与现有方法的定位对比

| 方法类型 | 代表 | 优点 | 缺点 |
|---------|------|------|------|
| 预训练先验 | AirNet, MiOIR | 退化表示准确 | 需要标注数据/预训练，泛化差 |
| 自适应提示 | PromptIR | 端到端简单 | 判别性不足，提示固定 |
| 文本/纹理提示 | 多模态方法 | 语义丰富 | 计算开销大 |
| **DPPD（本文）** | - | **判别性强 + 动态自适应 + 即插即用** | 推理有采样延迟 |

### 8.2 核心优势

1. **即插即用**: DPPD 可无缝集成到 PromptIR、SRResNet、Uformer 等不同骨干网络
2. **无需监督预训练**: 通过原型学习实现判别性，避免了标注数据的依赖
3. **动态性**: 分布建模 + 重参数化采样，表达能力远超固定提示
4. **复合退化能力突出**: 在 CDD-11 上提升达 +2.94 dB

### 8.3 已知局限

1. 预定义的原型集合可能无法完全覆盖未见或高度复杂的退化
2. 原型在训练中不动态更新（固定原型），可能限制对新颖退化的适应
3. 重参数化采样在推理时引入额外延迟（但相对性能提升而言开销较小）

### 8.4 未来方向

- 开发自适应原型分配机制，允许模型基于数据分布动态学习和更新原型
- 探索更高效的采样技术或实现优化以降低推理延迟